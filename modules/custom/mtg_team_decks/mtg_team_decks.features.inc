<?php
/**
 * @file
 * mtg_team_decks.features.inc
 */

/**
 * Implements hook_ctools_plugin_api().
 */
function mtg_team_decks_ctools_plugin_api($module = NULL, $api = NULL) {
  if ($module == "strongarm" && $api == "strongarm") {
    return array("version" => "1");
  }
}

/**
 * Implements hook_views_api().
 */
function mtg_team_decks_views_api($module = NULL, $api = NULL) {
  return array("api" => "3.0");
}

/**
 * Implements hook_node_info().
 */
function mtg_team_decks_node_info() {
  $items = array(
    'deck' => array(
      'name' => t('Deck'),
      'base' => 'node_content',
      'description' => '',
      'has_title' => '1',
      'title_label' => t('Name'),
      'help' => '',
    ),
  );
  drupal_alter('node_info', $items);
  return $items;
}
